package com.review.class29;

public class Recap {
	
	public static void main(String[] args) {
		
		int a=10; 
		int b=20;
		
		int[] array = {10, 20, 20, 30, 40} ;// elements inside the store we use indexes which starts from [0]
		
				System.out.println("size of array = " +array.length);
				
				int [] arr = new int [2];
				arr[0]=1;
				System.out.println("size of arr = " + arr.length);
				System.out.println(arr[0]);
				
				// inside the array I can store multiple values
				// can I add more value to the array later
				// arrays - data structure that are fixed in size and after that we cannot change it. if we change we will get arrayIndexOutOfBoundsException
				// Array is a static data structure:
				// we do not have a ready method that we can manipulate with data
				
			String [] words = {"Hello", "Welcome", "Thank you"};
			
				
	}

}
